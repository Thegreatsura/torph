---
"torph": patch
---

A word no longer character-morphs across a word that is staying put.

#### Fixed

- A resemblance only earns a character morph when both words sit in the same gap
  between surviving words. `Copy Address` → `Address Copied` no longer drags `Cop`
  the width of the value, past the `Address` holding still between them.
